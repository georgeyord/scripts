#!/bin/bash

export EDITOR=atom

alias e='sensible-editor'

alias 24='cd /opt/codebase/tripsta/app && which term-title && term-title 24'
alias frontline='cd /opt/codebase/tripsta/frontline && which term-title && term-title fl'
alias tpdocker='cd /opt/codebase/tripsta/docker/compose && which term-title && term-title tpdck'
alias codebase='cd /opt/codebase'
alias ydocker='cd /opt/codebase/dockerfiles && which term-title && term-title dockerfiles'
alias yscripts='cd /opt/codebase/scripts && which term-title && term-title scripts'

alias psauxgrep='ps aux | grep'
